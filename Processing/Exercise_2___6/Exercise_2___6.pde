// This sketch display errors
// The task is to find the errors
// All errors found have been commented

size(200, 200; // There is no closing bracket/parenthesis for this function

background(); // There is value/argument declared within the brackets

stroke 255; // Brackets are required when declaring arguments of a function

fill(150) // A semi-colon is missing the end the function

rectMode(center); // Determining the mode of a shape must be written all in capitals rectMode("CENTER");

rect(100, 100, 50); // This function syntax requires 4 arguments (x1, y1, width, height);
