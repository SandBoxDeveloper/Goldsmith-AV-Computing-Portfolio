// summary of functions

// this is the function make-up or better described as its structure
// ReturnType  FunctionName  (Arguments){
      //code body of function
//}

// 1st writing function without an return type or arguments

// void functionSummary () {
    //code here
//}

// 2nd writing an function with arguments

// void functionSummary (int a, int b) {
    //code here
    //rectMode(CENTER);
    //fill(0);
    //rect(int a, int b, 20, 20);
//}

//3rd writing an function with an return type


// float functionSummary (int a, int b) {
    //code here
    //rectMode(CENTER);
    //fill(0);
    //rect(int a, int b, 20, 20);
    //return functionSummary;
//}

//4th calling an function within an sketch

/*
void setup() {
      size(200, 200);
      background(255);
}

void draw() {
      println(functionSummary(5, 10));
      noLoop();
}

int functionSummary (int a, int b) {
    int tr = a * b;
    int tri = tr - a;
    return  tri;
}
*/

// functions should be constructed after the draw() function
// and if arguments are declared these must be passed into the function
// if an returnType is decalared it must have an return function at the end
